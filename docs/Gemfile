source "https://rubygems.org"

# GitHub Pages builds this site; the github-pages gem pins Jekyll and the
# plugins (including jekyll-remote-theme) to match the GitHub Pages build
# environment so local previews behave the same as the deployed site.
gem "github-pages", group: :jekyll_plugins
gem "jekyll-include-cache", group: :jekyll_plugins
